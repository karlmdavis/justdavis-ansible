---

# System timezone, as a tz database name. Servers stay on UTC, the default, because systemd timers fire
# in local time: changing a server's timezone silently shifts every scheduled job. Workstations override
# this in their group_vars.
base_timezone: Etc/UTC
