---

# System timezone, as a tz database name. UTC is the default because every systemd timer's `OnCalendar` is
# in local time, so a server's timezone silently sets when its scheduled jobs run; workstations override
# this in their group_vars.
base_timezone: Etc/UTC
