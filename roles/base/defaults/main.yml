---

# System timezone, as a tz database name. Servers should stay on UTC (override in their host_vars): every
# systemd timer's `OnCalendar` is in local time, so changing a server's timezone silently moves its
# scheduled jobs.
base_timezone: America/New_York
